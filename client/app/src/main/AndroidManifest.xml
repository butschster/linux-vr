<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 6DoF-трекинг обязателен: слой прибивается к мировым координатам -->
    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="true"
        android:version="1" />

    <!-- Без этой строки Horizon OS блокирует запуск с диалогом
         "нужны контроллеры", если пользователь работает руками.
         Для VR-десктопа контроллеры лежат на столе — это норма, а не исключение. -->
    <uses-feature
        android:name="oculus.software.handtracking"
        android:required="false" />
    <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:label="linux-vr">

        <meta-data
            android:name="com.oculus.supportedDevices"
            android:value="quest3|quest3s" />

        <activity
            android:name="android.app.NativeActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:excludeFromRecents="false"
            android:configChanges="density|keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|uiMode"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">

            <meta-data
                android:name="android.app.lib_name"
                android:value="linuxvr" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Категория IMMERSIVE_HMD запускает приложение сразу в VR, минуя 2D-панель -->
                <category android:name="org.khronos.openxr.intent.category.IMMERSIVE_HMD" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
